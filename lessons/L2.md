# L2 — 真實課表上線　⏱ ~50 分

**本課完課你會看到**：後台貼 CSV 一鍵匯入課表；前台顯示本週真實課表與剩餘名額。
詳細見根目錄 `L2-課表上線-操作手冊.md`。

---

### 🧰 設定關卡（老師，課前準備）— 課表轉 CSV
把教室 Canva 課表圖丟 Gemini，請它轉成 CSV（欄位固定 `date,time,title,instructor,capacity`，看不清標 `???` 不准猜）。人工補完 `???`。

### 👩‍💻 給 agy 的指令（這一課）
```
只做 L2：
1) 建後台 admin.html：Google 登入，email≠STUDIO_EMAIL 擋住；貼 CSV(date,time,title,instructor,capacity) → 寫 courses，
   文件ID=date去「-」+"_"+time去「:」，enrolled=0、status="open"、capacity 轉數字；寫入前先讀，已存在不覆寫 enrolled/status。
2) 前台 index.html 顯示「本週課表」：查本週 date 區間且 status=="open"，依 date,time 排序，顯示剩餘名額（先不放預約鈕）。
欄位一律照 安裝包/firestore-資料模型.md。做完停下。不要做 LIFF 或預約。
```

### ✅ 驗收
- 後台登入、貼 CSV、匯入成功；Firestore 出現 courses（ID 如 `20260625_1900`）。
- 前台看到本週課與名額；無資料顯示「本週尚無課程」。

### 🛑 STOP
本課結束。等「開始 L3」。
