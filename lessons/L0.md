# L0 — 網站上線（PWA 空殼）　⏱ ~60 分

**本課完課你會看到**：`你的帳號.github.io/<repo>/` 打得開，顯示「課程預約系統」，手機可加到主畫面。
詳細點擊步驟見根目錄 `課程總設計與L0操作手冊.md` 第五節。

---

### 🧰 設定關卡 1（老師，課程開始）— 建 GitHub repo ＋ gh 授權
agy 做不到，要你登入 GitHub：
1. github.com → New repository → 名稱自取 → **Public**（免費帳號要跑 Pages）→ 不勾任何初始檔 → Create。
2. 一次性 `gh` 授權（**取代所有 PAT／token 做法，零明碼**）：
   ```
   brew install gh        # 若尚未安裝
   gh auth login          # 選 GitHub.com → HTTPS → Login with a web browser
   ```
3. 終端機接遠端：
   ```
   git init && git branch -M main
   git remote add origin https://github.com/<你>/<repo>.git
   ```
> 先別 push，等本課做完再 push。**永遠不要產 PAT、不要建 .env、不要寫 deploy.sh**——push 靠 gh 憑證即可。

### 👩‍💻 給 agy 的指令（這一課）
```
只做 L0：建 index.html（置中卡片「課程預約系統」+ 一行說明，手機優先、內嵌樣式、無外部 CSS）、
manifest.webmanifest（standalone、icons 192+512 含 maskable）、sw.js（同源 network-first、外部不攔截、CACHE="v1"）、
用 Python Pillow 畫純幾何 App 圖示輸出 icon-192/512.png，並在 index.html 掛 manifest/theme-color/apple-touch-icon 與註冊 sw.js。
做完停下，告訴我怎麼驗收。不要做 Firebase 或任何之後的功能。
```

### 🧰 設定關卡 2（老師，本課末）— 開 GitHub Pages
做完 commit + `git push -u origin main` → repo Settings → Pages → Source `main`/(root) → Save。

### ✅ 驗收
- 手機開 Pages 網址看到卡片。
- iPhone 用 **Safari** 開 → 分享 ⬆️ → 加入主畫面（從 LINE 點是內建瀏覽器，沒這選項）。

### 🛑 STOP
本課結束。等老師說「開始 L1」再繼續，**不要先碰 Firebase**。
