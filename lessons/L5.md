# L5 — 取消預約　⏱ ~40 分

**本課完課你會看到**：在「我的預約」按取消 → 退一堂、名額 +1，該堂釋出。
詳細見根目錄 `L5-取消預約-操作手冊.md`。

---

### 👩‍💻 給 agy 的指令（這一課）
```
只做 L5：在 index.html 加「我的預約」清單（查 bookings lineUserId==currentUserId 且 status=="booked"，顯示課名/日期/時間）。
每筆「取消」鈕，距開課<2 小時則 disabled。點取消先確認 → 用 booking.js 的 cancelBooking(courseId,currentUserId) → 成功提示「已退回 1 堂」、即時移除該筆、堂數 +1。
沒有預約顯示「目前沒有預約的課」。做完停下。不要做後台(L6)。
```

### ✅ 驗收
- 取消後：清單少一筆、堂數 +1、課程 `enrolled` −1、booking status=cancelled。
- 釋出的名額別的帳號可預約。
- 距開課 <2 小時的課，取消鈕 disabled。

### 🛑 STOP
本課結束。等「開始 L6」。
